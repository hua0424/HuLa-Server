<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luohuo.flex.im.core.chat.mapper.RoomMapper">

    <select id="groupList" resultType="com.luohuo.flex.im.domain.vo.response.MemberResp">
        select distinct r.id     as roomId,
               g.name   as groupName,
               g.avatar as avatar,
               g.id     as groupId,
               g.account,
               g.allow_scan_enter,
               r.update_time
        from im_room_group g
                 inner join im_room r on g.room_id = r.id
                 inner join im_group_member m on g.id = m.group_id and m.is_del = 0
        where m.uid = #{uid}
          and g.is_del = 0
        order by r.update_time,
                 g.id
    </select>

    <select id="getAllGroupList" resultType="com.luohuo.flex.im.domain.vo.response.MemberResp">
        select distinct r.id     as roomId,
               g.name   as groupName,
               g.avatar as avatar,
               g.id     as groupId,
               g.account,
               g.allow_scan_enter,
               r.update_time
        from im_room_group g
                 inner join im_room r on g.room_id = r.id
        where g.is_del = 0
        order by r.update_time desc,
                 g.id
    </select>

    <!-- ISS-005: last_msg_id / active_time 同时加 IF 单调保护
         - sendMsg 同事务 sync 写入 与 MsgSendConsumer 异步写入并发时,IF 防止旧 msgId / 旧 sendTime 覆盖新值
         - skipPush=true(stream_end)路径不发 MessageSendEvent,由 sendMsg 兜底单独推进
         - 与 ContactMapper.refreshOrCreateActiveTime(ISS-004)保持同构 -->
    <update id="refreshActiveTime">
        UPDATE im_room
        SET last_msg_id = IF(#{msgId} &gt; last_msg_id OR last_msg_id IS NULL, #{msgId}, last_msg_id),
            active_time = IF(#{msgTime} &gt; active_time OR active_time IS NULL, #{msgTime}, active_time)
        WHERE id = #{roomId}
    </update>
</mapper>
